version: "2.1"
dependencies:
  - project: kiegroup/drools
    mapping:
      dependant:
        default:
          - source: 8.36.x
            target: 1.0.x

  - project: kiegroup/drools-ansible-rulebook-integration
    dependencies:
      - project: kiegroup/drools
    mapping:
      dependencies:
        default:
          - source: 1.0.x
            target: 8.36.x
